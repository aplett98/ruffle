import { Game } from "./game"

describe("Game", () => {
    let game: Game

    beforeEach(() => {
        game = new Game("flame", "prong");
    })

    it("initializes the game correctly", () => {
        
    })
})